{
  "name": "rydway-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run start:dev --prefix backend\"",
    "install:all": "npm install && npm install --prefix client && npm install --prefix backend"
  },
  "dependencies": {
    "next-themes": "^0.4.6",
    "sonner": "^2.0.7"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
